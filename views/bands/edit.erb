<section id="edit_band">
  <form method="POST" action="/bands/<%= @id %>/edit" >
    <input type="hidden" name="_method" value="PUT" />
    Band
    <input type="text" name="band[name]" value="<%= @band.name %>"/>
  </br>
    Genre
    <input type="text" name="band[genre]" value="<%= @band.genre %>"/>
  </br>
    Still Relevant
    <input type="radio" name="band[currently_relevant]" value="<%= @band.currently_relevant %>">Yes
    <input type="radio" name="band[currently_relevant]" value="<%= @band.currently_relevant %>">No
  </br>
    <input type="submit" value="submit"/>
  </form>
</section>
